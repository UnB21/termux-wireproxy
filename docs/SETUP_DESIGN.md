# twp setup Design

## Purpose

The setup command provides a beginner-friendly first-run experience for Termux WireProxy.

The goal is to allow a new user to configure the project without manually editing configuration files.

---

# Command

Primary command:
